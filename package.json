{
  "name": "ai-coder-webapp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "start": "node index.js",
    "setup": "npm install && npm install --prefix website-react && npm install --prefix website-react/client && npm install --prefix website-react/server && npm install --prefix website-vulnerable && npm install --prefix website-vulnerable/client && npm install --prefix website-vulnerable/server"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@babel/eslint-parser": "^7.27.0",
    "eslint": "^9.0.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^5.2.0"
  }
}